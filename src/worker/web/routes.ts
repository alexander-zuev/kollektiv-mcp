export const AppRoutes = {
	ALL: "*",
	ROOT: "/",
	AUTHORIZE: "/authorize",
	AUTH_CONFIRM: "/auth/confirm",
	AUTH_CALLBACK: "/auth/callback",
	LOGIN: "/login",
	LOGOUT: "/logout",
	SSE: "/sse",
} as const;
